# Dados (gigabytes, reprodutíveis a partir da fonte)
data/
*.zip
*.csv
*.parquet
*.npy
*.duckdb

# Exceção: as fixtures são amostras de poucos KB, versionadas de propósito.
# Sem esta linha o padrão *.csv acima as engole em silêncio, e a suíte quebra
# só no primeiro clone limpo.
!tests/fixtures/*.csv

# Ambiente
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Sistema
.DS_Store
Thumbs.db
